# Copy to .env and fill in real values. .env is gitignored.

# Prod postgres via SSH tunnel
DB_HOST=localhost
DB_PORT=15432
DB_USER=
DB_PASSWORD=
DB_NAME=rektfree

# SSH tunnel target
VM_HOST=
VM_USER=deploy
SSH_KEY=~/.ssh/id_ed25519
LOCAL_PG_PORT=15432

# OANDA (for direct API refetch of forex/indices)
OANDA_API_KEY=
OANDA_ACCOUNT_ID=
OANDA_BASE_URL=https://api-fxpractice.oanda.com

DATA_DIR=data

# Telegram signal delivery (paper trading)
TELEGRAM_BOT=
CHAT_ID=
